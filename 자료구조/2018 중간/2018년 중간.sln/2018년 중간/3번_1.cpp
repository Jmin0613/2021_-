// 15라는 데이터 값을 가지는 노드를 10과 20 사이에 삽립하려함.
class LinkedList {
	Node org;
publice:
	LinkedList() org(0) {}
	~LinkedList() { clear(); }
	void clear()
};

# include "LinkedList.h"
void main() {
	LinkedList list;
	list.insert(0, new Node(10));
	list.insert(1, new Node(20));
	list.insert(1, neew Node(20));
	list.display()
}