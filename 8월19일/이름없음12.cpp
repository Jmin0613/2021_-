/*
리터럴(literal) : 소스코드 상에서 고정된 값을 가지는 것 
고정됨 -> 변경 불가 
문자열 리터럴(string literal)
*/

#include <stdio.h>

int main(){
	char *pstr="goodbye";
	printf("why so serious?");
	scanf("%c", str[0]);
	
	return 0;
}

/*
1. 문자열 내 총 문자 수 세는 함수
2. 문자열 복사 함수
3. 문자열 합치는 함수
4. 문자열 비교 함수
*/
